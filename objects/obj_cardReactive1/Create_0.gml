omniscient = false; //Whether the card can react while in the deck/recycle
idleScript = noone; //Ran every step to determine what the card does while idle

// Inherit the parent event
event_inherited();